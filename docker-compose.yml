version: '3.8'

services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: darpay-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-darpay_secure_password}
      MYSQL_DATABASE: ${DB_NAME:-darpay_mock_db}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/my.cnf:/etc/mysql/conf.d/custom.cnf:ro
    ports:
      - "3306:3306"
    networks:
      - darpay-network
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1024M
        reservations:
          cpus: '0.5'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Payout API
  payout-api:
    build:
      context: .
      dockerfile: services/payout-api/Dockerfile
      args:
        - DATABASE_URL=mysql://root:dummy@localhost:3306/dummy
    container_name: darpay-payout-api
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=4001
      - DATABASE_URL=mysql://root:${DB_PASSWORD:-darpay_secure_password}@mysql:3306/${DB_NAME:-darpay_mock_db}
      - WEBHOOK_SECRET=${WEBHOOK_SECRET:-default-webhook-secret-key}
      - NODE_OPTIONS=--max-old-space-size=256
    ports:
      - "4001:4001"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - darpay-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 384M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Payout Worker (Background Processing)
  payout-worker:
    build:
      context: .
      dockerfile: services/payout-api/Dockerfile
      args:
        - DATABASE_URL=mysql://root:dummy@localhost:3306/dummy
    container_name: darpay-payout-worker
    restart: unless-stopped
    command: ["pnpm", "--filter", "@darpay/payout-api", "run", "start:worker"]
    environment:
      - NODE_ENV=production
      - DATABASE_URL=mysql://root:${DB_PASSWORD:-darpay_secure_password}@mysql:3306/${DB_NAME:-darpay_mock_db}
      - WEBHOOK_SECRET=${WEBHOOK_SECRET:-default-webhook-secret-key}
      - NODE_OPTIONS=--max-old-space-size=256
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - darpay-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 384M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Payment API
  payment-api:
    build:
      context: .
      dockerfile: services/payment-api/Dockerfile
      args:
        - DATABASE_URL=mysql://root:dummy@localhost:3306/dummy
    container_name: darpay-payment-api
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=4002
      - DATABASE_URL=mysql://root:${DB_PASSWORD:-darpay_secure_password}@mysql:3306/${DB_NAME:-darpay_mock_db}
      - JWT_SECRET=${JWT_SECRET:-supersecretjwtkey}
      - API_KEY_SECRET=${API_KEY_SECRET:-supersecretapikey}
      - PAYMENT_PAGE_URL=${PAYMENT_PAGE_URL:-https://payment-darpay.vstore.cloud}
      - NODE_OPTIONS=--max-old-space-size=256
    ports:
      - "4002:4002"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - darpay-network
    deploy:
      resources:
        limits:
          cpus: '0.75'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 384M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Merchant Portal
  merchant-portal:
    build:
      context: .
      dockerfile: services/merchant-portal/Dockerfile
    container_name: darpay-merchant-portal
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=4010
      - NEXT_PUBLIC_API_URL=https://api-darpay.vstore.cloud
      - NODE_OPTIONS=--max-old-space-size=256
    ports:
      - "4010:4010"
    depends_on:
      - payment-api
    networks:
      - darpay-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 384M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Admin Portal
  admin-portal:
    build:
      context: .
      dockerfile: services/admin-portal/Dockerfile
    container_name: darpay-admin-portal
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=4011
      - NEXT_PUBLIC_API_URL=https://api-darpay.vstore.cloud
      - NODE_OPTIONS=--max-old-space-size=256
    ports:
      - "4011:4011"
    depends_on:
      - payment-api
      - payout-api
    networks:
      - darpay-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 384M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Payment Page (Customizable checkout page)
  payment-page:
    build:
      context: .
      dockerfile: services/payment-page/Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-https://test.darpay.mx}
    container_name: darpay-payment-page
    restart: unless-stopped
    ports:
      - "4012:80"
    depends_on:
      - payment-api
    networks:
      - darpay-network
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
        reservations:
          cpus: '0.1'
          memory: 64M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  darpay-network:
    driver: bridge

volumes:
  mysql_data:
